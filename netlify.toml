[build]
  publish = "."                # Root directory (index.html is here)
  functions = "netlify/functions"

[dev]
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"     # ensures dependencies like node-fetch work
